MEMORY {
	RAM:	start = $07FF, size = $2000;
	BANK0:	start = $A000, size = $2000;
	BANK1:	start = $A000, size = $2000;
	BANK2:	start = $A000, size = $2000;
}

SEGMENTS {
	CODE:	load = RAM, type = ro;
	RODATA: load = RAM, type = ro;
	DATA:	load = RAM, type = rw;
	BSS:	load = RAM, type = bss;
	BANK0:	load = RAM, run = BANK0, type = ro, define = yes;
	BANK1:	load = RAM, run = BANK1, type = ro, define = yes;
	BANK2:	load = RAM, run = BANK2, type = ro, define = yes;
}
